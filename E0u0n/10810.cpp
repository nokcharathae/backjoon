#include <iostream>

using namespace std;

int main(){
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    
    int N,M,index;
    for(int i=1; i<=9; i++)
    {
        
    }

    cout<<max<<'\n'<<index;

    return 0;
}